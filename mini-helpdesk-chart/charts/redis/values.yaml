image:
  repository: redis
  tag: "7.2"
  pullPolicy: IfNotPresent

service:
  port: 6379

auth:
  password: ref+vault://minihelpdesk/redis#REDIS_PASSWORD

persistence:
  enabled: true
  size: 1Gi
