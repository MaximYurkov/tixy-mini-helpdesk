{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: pg-config
  namespace: {{ .Release.Namespace }}
data:
  POSTGRES_HOST: "{{ .Values.serviceName }}"
  POSTGRES_PORT: "{{ .Values.port }}"
{{- end }}
